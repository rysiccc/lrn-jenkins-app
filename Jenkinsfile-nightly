pipeline {
    agent any
    stages {
        stage('Build Playwright Docker image') {
            agent any
            steps {
                sh '''
                    docker build -f ci/Dockerfile -t my-playwright:latest .
                '''
            }
        }
        // ...tutaj kolejne kroki pipeline nightly...
    }
}
